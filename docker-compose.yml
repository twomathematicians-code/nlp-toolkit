services:
  nlp-api:
    build: .
    ports: ["8000:8000"]
    volumes:
      - spacy_models:/app/models
    environment:
      SPACY_MODEL: en_core_web_lg
      CACHE_TTL: 3600
    restart: unless-stopped

volumes:
  spacy_models:
